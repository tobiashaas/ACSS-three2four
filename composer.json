{
    "name": "tobiashaas/acss3-to-4",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=8.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10"
    },
    "scripts": {
        "test": "phpunit",
        "lint": "@php -r \"foreach (array_filter(explode(PHP_EOL, trim(shell_exec('git ls-files \\\"*.php\\\"')))) as $file) { passthru('php -l ' . escapeshellarg($file), $code); if ($code !== 0) { exit($code); }}\""
    }
}
